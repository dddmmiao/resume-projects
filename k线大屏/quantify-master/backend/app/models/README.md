# Models 目录结构

## 📁 目录组织

```
models/
├── __init__.py                 # 主入口文件，导出所有公共接口
├── base/                       # 基础模块
│   ├── __init__.py
│   ├── database.py             # 数据库连接和会话管理
│   ├── table_factory.py        # 表工厂，提供统一的表模型获取接口
│   └── table_strategy.py       # 表策略配置
├── entities/                   # 实体模型
│   ├── __init__.py
│   ├── stock.py                # 股票基本信息模型
│   ├── convertible_bond.py     # 可转债基本信息模型
│   ├── concept.py              # 概念板块模型
│   ├── trade_calendar.py       # 交易日历模型
│   └── sort_metrics.py         # 排序指标模型
├── klines/                     # K线数据模型
│   ├── __init__.py
│   ├── stock_kline.py          # 股票K线数据基类
│   ├── convertible_bond_kline.py # 可转债K线数据基类
│   ├── concept_kline.py        # 概念K线数据基类
│   └── industry_kline.py       # 行业K线数据基类
└── management/                 # 管理模块
    ├── __init__.py
    ├── dynamic_table_manager.py # 动态表管理器
    └── startup_table_initializer.py # 启动时表初始化器
```

## 🎯 模块职责

### base/ - 基础模块
- **database.py**: 数据库连接、引擎、会话管理
- **table_factory.py**: 统一的表模型获取接口，支持按年份动态创建表
- **table_strategy.py**: 表策略配置，定义分表规则

### entities/ - 实体模型
- **stock.py**: 股票基本信息（代码、名称、行业等）
- **convertible_bond.py**: 可转债基本信息（代码、名称、发行信息等）
- **concept.py**: 概念板块信息
- **trade_calendar.py**: 交易日历（交易日、节假日等）
- **sort_metrics.py**: 排序指标（涨跌幅、成交量等）

### klines/ - K线数据模型
- **stock_kline.py**: 股票K线数据基类（OHLCV、技术指标等）
- **convertible_bond_kline.py**: 可转债K线数据基类
- **concept_kline.py**: 概念K线数据基类
- **industry_kline.py**: 行业K线数据基类

### management/ - 管理模块
- **dynamic_table_manager.py**: 动态表管理器，按年份自动创建分表
- **startup_table_initializer.py**: 系统启动时预建所有必要的表

## 🔄 导入方式

### 从主模块导入
```python
from app.models import Base, Stock, get_stock_kline_table
```

### 从子模块导入
```python
from app.models.base import database, table_factory
from app.models.entities import Stock, ConvertibleBond
from app.models.klines import StockKlineDataBase
from app.models.management import DynamicTableManager
```

## ⚠️ 注意事项

1. **循环导入**: 已通过延迟导入解决循环依赖问题
2. **兼容性**: 保持了原有的公共接口，确保现有代码无需修改
3. **扩展性**: 新模块可以轻松添加到相应目录中
4. **维护性**: 清晰的目录结构便于代码维护和查找

## 📝 更新记录

- 2024-10-15: 重构目录结构，按功能模块分类
- 解决循环导入问题
- 保持向后兼容性
