# watchRGB 项目架构文档

> 📱 **watchRGB** - Apple Watch 专属的 RGB 颜色显示与呼吸灯应用

---

## 📋 项目概述

**watchRGB** 是一款面向 Apple Watch 的专业 RGB 颜色显示应用。用户可以通过多种交互方式（色轮、数字输入、表情符号）选择任意颜色，并支持单色显示和呼吸灯两种模式。应用充分利用 watchOS 的数字表冠和手腕动作检测功能，提供流畅的交互体验。

### 核心功能
- 🎨 **三种颜色选择方式**：色轮、RGB数字输入、表情符号
- 💫 **双显示模式**：单色模式 / 呼吸灯模式
- 👑 **表冠精细控制**：调节饱和度和亮度
- 📳 **摇晃随机切换**：晃动手表随机生成颜色
- 🔋 **电池保护机制**：低电量自动限速
- 💎 **内购解锁**：高级功能付费解锁

---

## 📁 目录结构

```
watchRGB Watch App/
├── Application/          # 🚀 应用入口层
│   └── watchRGBApp.swift           # App入口点，初始化全局状态
│
├── Config/               # ⚙️ 配置层
│   └── GlobalConfig.swift          # 全局配置：颜色、间距、字体、动画参数
│
├── Models/               # 📦 数据模型层
│   ├── ColorModels.swift           # ColorInfo颜色模型、颜色持久化
│   ├── ColorSelectorMode.swift     # 颜色选择器模式枚举
│   └── DisplayModes.swift          # 显示模式、呼吸模式枚举
│
├── Managers/             # 🧠 状态管理层（核心业务逻辑）
│   ├── DisplayModeManager.swift    # ⭐ 核心：显示模式状态机、呼吸动画
│   ├── StoreKitManager.swift       # 内购管理（StoreKit 2）
│   ├── MembershipManager.swift     # 会员权限控制
│   ├── BatteryMonitor.swift        # 电池监控、速度限制
│   ├── BreathingTimeLimitManager.swift  # 呼吸模式时间限制
│   ├── BrightnessManager.swift     # 亮度状态管理
│   └── TutorialManager.swift       # 新手引导管理
│
├── Handlers/             # 🎮 交互处理层
│   ├── GestureHandlers.swift       # ⭐ 核心：手势处理（滑动、点击、长按）
│   ├── CrownRotationHandler.swift  # ⭐ 表冠旋转处理（watchOS特色）
│   ├── InteractionHandlers.swift   # 交互回调处理
│   ├── NotificationHandlers.swift  # 通知系统、组件通信
│   ├── ShakeGestureHandler.swift   # 摇晃检测（CoreMotion）
│   └── ShakeHandler.swift          # 摇晃响应逻辑
│
├── Utils/                # 🔧 工具层
│   ├── ColorFormatter.swift        # 颜色格式化（RGB/HEX/Emoji）
│   └── ColorUtilities.swift        # 颜色转换工具函数
│
├── Views/                # 🎨 视图层
│   ├── Main/
│   │   └── ContentView.swift       # ⭐ 主视图（1800+行，应用核心）
│   │
│   ├── ColorSelectors/             # 颜色选择器组件
│   │   ├── ColorWheel.swift        # ⭐ 色轮选择器（极坐标计算）
│   │   ├── RGBInputView.swift      # RGB滚轮输入
│   │   ├── EmojiColorSelector.swift# 表情符号选择器
│   │   └── ColorSelectorContainerView.swift  # 选择器容器
│   │
│   ├── Components/                 # 通用UI组件
│   │   ├── ButtonViews.swift       # 自定义按钮组件
│   │   └── CapsuleToast.swift      # Toast提示组件
│   │
│   └── Settings/                   # 设置页面
│       ├── SettingsView.swift      # 设置主页面
│       ├── OptimalSettingsGuideView.swift   # 设置指南
│       └── TutorialReplayView.swift         # 引导重播
│
└── Resources/            # 🌍 资源层
    ├── zh-Hans.lproj/              # 简体中文
    ├── en.lproj/                   # 英语
    ├── ja.lproj/                   # 日语
    └── ...                         # 其他语言
```

---

## 🏗️ 架构设计

### 整体架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                        watchRGBApp                               │
│                     (应用入口 @main)                             │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                       ContentView                                │
│                      (主视图容器)                                │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────┐  │
│  │ 背景颜色层  │  │  颜色选择器 │  │    控制按钮层           │  │
│  │            │  │   容器      │  │ (添加/删除/设置)        │  │
│  └─────────────┘  └─────────────┘  └─────────────────────────┘  │
└──────────────────────────┬──────────────────────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌───────────────┐  ┌───────────────┐  ┌───────────────┐
│   Handlers    │  │   Managers    │  │    Views      │
│  (交互处理)   │  │  (状态管理)   │  │   (视图组件)   │
│               │  │               │  │               │
│ •GestureH.    │  │ •DisplayMode  │  │ •ColorWheel   │
│ •CrownH.      │  │ •StoreKit     │  │ •RGBInput     │
│ •ShakeH.      │  │ •Membership   │  │ •EmojiSelect  │
│ •Notification │  │ •Battery      │  │ •Settings     │
└───────────────┘  └───────────────┘  └───────────────┘
```

### 设计模式

| 模式 | 应用场景 | 实现示例 |
|------|----------|----------|
| **单例模式** | 全局状态管理 | `GlobalConfig.shared`, `ToastManager.shared` |
| **观察者模式** | 组件间通信 | `NotificationCenter` 通知系统 |
| **状态机模式** | 模式切换 | `DisplayMode`, `ColorSelectorMode` 枚举 |
| **策略模式** | 颜色格式化 | `ColorFormatter` 多种格式支持 |
| **扩展模式** | 代码组织 | `ContentView` 按功能拆分到多个 extension |

---

## 🔄 数据流

### 颜色选择数据流

```
用户交互 (手势/表冠/滚轮)
         │
         ▼
┌─────────────────────┐
│    Handlers 层      │  捕获并处理用户输入
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│  ContentView @State │  更新视图状态变量
│  • customColor      │  (customHue, customSaturation, customBrightness)
│  • isCustomColor    │
└─────────┬───────────┘
          │
          ▼
┌─────────────────────┐
│   视图自动刷新      │  SwiftUI 响应式更新UI
│   • 背景颜色变化    │
│   • Toast提示显示   │
└─────────────────────┘
```

### 呼吸模式动画流

```
DisplayModeManager
         │
    Timer(0.02s)                    ┌──────────────────────┐
         │                          │  颜色插值算法        │
         ▼                          │  smoothStep(t)       │
┌─────────────────┐                 │  = 3t² - 2t³         │
│ transitionProgress += 0.005×speed │  (平滑过渡曲线)      │
└────────┬────────┘                 └──────────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────┐
│ currentBreathingColor = interpolate(from, to)  │
└────────┬────────────────────────────────────────┘
         │
         ▼
   视图背景色更新
```

---

## 🔑 核心技术点

### 1. 数字表冠精细控制 (watchOS 特色)
```swift
// 表冠旋转 → 饱和度/亮度控制
.digitalCrownRotation($crownValue, from: 0, through: 1, sensitivity: .medium)
.onChange(of: crownValue) { handleCrownRotation(oldValue, newValue) }
```

**技术要点**：
- 惯性检测：防止表冠惯性导致的误操作
- 保护窗口：触摸结束后短暂忽略表冠输入
- 双模式切换：色轮内控制饱和度，亮度环内控制亮度

### 2. CoreMotion 摇晃检测
```swift
motionManager.accelerometerUpdateInterval = 0.05
motionManager.startAccelerometerUpdates(to: .main) { data, error in
    let magnitude = sqrt(x² + y² + z²)
    if magnitude > shakeThreshold && timeSinceLast > cooldown {
        onShakeDetected()
    }
}
```

**技术要点**：
- 采样频率：50ms（20Hz）
- 阈值可配置：用户可调节灵敏度
- 冷却时间：防止连续误触发

### 3. HSB 色轮极坐标计算
```swift
// 触摸点 → 颜色值
let angle = atan2(dy, dx)
let hue = (angle / (2 * .pi) + 0.5).truncatingRemainder(dividingBy: 1.0)
let saturation = min(distance / radius, 1.0)
```

### 4. 电池保护机制
```swift
// 根据电量动态限制刷新速度
func getMaxAllowedSpeed() -> Double {
    switch batteryLevel {
    case 0.3...1.0: return 50    // 满电无限制
    case 0.2..<0.3: return 20    // 中等限制
    case 0.1..<0.2: return 10    // 严格限制
    default: return 5            // 极限保护
    }
}
```

### 5. StoreKit 2 内购
```swift
// 现代化的 async/await 内购流程
let result = try await product.purchase()
switch result {
case .success(let verification):
    let transaction = try checkVerified(verification)
    await transaction.finish()
case .userCancelled, .pending: break
}
```

---

## 📊 文件统计

| 模块 | 文件数 | 总行数 | 核心文件 |
|------|--------|--------|----------|
| Application | 1 | ~25 | watchRGBApp.swift |
| Config | 1 | ~315 | GlobalConfig.swift |
| Models | 3 | ~480 | DisplayModes.swift |
| Managers | 7 | ~1150 | DisplayModeManager.swift |
| Handlers | 6 | ~1250 | GestureHandlers.swift |
| Utils | 2 | ~115 | ColorUtilities.swift |
| Views | 10 | ~4500 | ContentView.swift |
| **总计** | **30** | **~8000** | - |

---

## 🌐 多语言支持

支持 7 种语言：
- 🇨🇳 简体中文 (zh-Hans)
- 🇺🇸 英语 (en)
- 🇯🇵 日语 (ja)
- 🇰🇷 韩语 (ko)
- 🇫🇷 法语 (fr)
- 🇩🇪 德语 (de)
- 🇪🇸 西班牙语 (es)

---

## 🛡️ 质量保障

- **设备自适应**：基于屏幕尺寸的 scaleFactor 动态计算
- **硬件保护**：电池监控 + 使用时长限制
- **用户体验**：触觉反馈 (WKInterfaceDevice.play)
- **状态持久化**：UserDefaults 保存用户偏好

---

*本文档由 AI 自动生成，用于项目理解和简历展示。*
